# Providers

## Ollama

Local default. Base URL `http://127.0.0.1:11434`.

## OpenAI-compatible

Set base URL + API key. Works with OpenAI and compatible services (test xAI Grok API features individually).

## Gemini native

Use official developer API key from Google Cloud / AI Studio — never browser cookies.
