# Share diagnostics checklist

Before sharing logs or screenshots:

1. Remove API keys and tokens.
2. Remove home directory paths that reveal personal names if undesired.
3. Confirm no `.env` contents.
4. Prefer redacted exports from the app (when available).
5. Do not attach full workspace archives.